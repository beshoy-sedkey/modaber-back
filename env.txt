# ═══════════════════════════════════════════════
# E-Commerce AI Automation System — Environment Variables
# ═══════════════════════════════════════════════

# ─── Database (auto-configured by docker-compose) ───
DATABASE_URL=postgresql://ecommerce_admin:dev_password_123@postgres:5432/ecommerce_ai?schema=public

# ─── Redis (auto-configured by docker-compose) ───
REDIS_URL=redis://redis:6379

# ─── App ───
NODE_ENV=development
PORT=3000

# ─── JWT ───
JWT_SECRET=dev_jwt_secret_change_this_in_production_to_random_64_chars
JWT_EXPIRATION=24h

# ─── Encryption (for PII fields) ───
ENCRYPTION_KEY=dev_encryption_key_32_bytes_change

# ─── OpenAI (for AI Chat Agent + Embeddings) ───
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here

# ─── Shopify ───
# Get from: https://partners.shopify.com → Apps → Your App → API credentials
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SHOPIFY_SCOPES=read_products,write_products,read_orders,write_orders,read_customers

# ─── Salla ───
# Get from: https://salla.partners → My Apps → Your App → App Keys
SALLA_CLIENT_ID=
SALLA_CLIENT_SECRET=
SALLA_WEBHOOK_TOKEN=

# ─── WhatsApp (Meta Cloud API) ───
# Get from: https://developers.facebook.com → Your App → WhatsApp → API Setup
WHATSAPP_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=your_verify_token
WHATSAPP_BUSINESS_ACCOUNT_ID=
